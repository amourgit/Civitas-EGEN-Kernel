package africa.civitas.egen.modules.business.organization.api.politique.domain;

/** Domaine d'une Politique — fixe au niveau plateforme, comme NatureTutelle (A2). */
public enum DomainePolitique {
    SECURITE_MOT_DE_PASSE,
    FORMAT_DATE,
    LANGUE_PAR_DEFAUT,
    REGLE_METIER_PROPRE,
    CONFIDENTIALITE,
    RETENTION_DONNEES,
    AUTRE
}
